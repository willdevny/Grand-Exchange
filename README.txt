Drop these files into your project root and replace the existing conflicted files:

- app/api/agent/context/route.ts
- app/api/stock/[symbol]/route.ts

Then run:
git add .
git commit -m "Resolve merge conflicts and keep Reddit/social feature"
git push -u origin main
